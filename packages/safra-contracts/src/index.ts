/**
 * @colheita/safra-contracts
 *
 * Schemas Zod e tipos TypeScript para integração com o sistema Safra.
 * Compartilhado entre apps/api (webhook receiver) e qualquer consumer
 * futuro (jobs, portal, admin).
 */

export * from './events.js';
